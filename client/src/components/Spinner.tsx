import React from 'react';
import { SpinnerDiamond } from 'spinners-react';

const Spinner = (): JSX.Element => (
  <SpinnerDiamond size={70} speed={150} color="#ffffff" secondaryColor="#000000" />
);

export default Spinner;
